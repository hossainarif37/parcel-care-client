const Loading = () => {
    return (
        <div className={'min-h-screen flex justify-center items-center'}>
            <div className="animate-spin w-16 h-16 border-t border-secondary rounded-full" />
        </div>
    );
};

export default Loading;